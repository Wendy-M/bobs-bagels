const Basket =
    require('../src/basket');
const assertEqual =
    require('../test/test-framework')

let basket;

// Arrange 
basket = new Basket(0);
expectedOutput = false;

//Act 
actualOutput =
    basket.isBasketFull();
    reduceNumberOfItems();

// Assert 
result =
    assertEquals(expectedOutput, actualOutput);
    
    
    
    
    
    class Basket  {
    //properties
    numberOfItems;
    
    constructor(numberOfItems, capacity) {
        // set values for object instance 
        this.numberOfItems = numberOfItems
        this.capacity = capacity
    }
    // messages 
    isBasketFull() { }
    reduceNumberOfItems() { }
    addItemToBasket() { }
}
